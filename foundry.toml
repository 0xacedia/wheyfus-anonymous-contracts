[profile.default]
src = 'src'
out = 'out'
libs = ['lib']
gas_reports = ['Bonding', 'Wheyfu', 'PuttyV2']
optimizer_runs = 1000
solc_version = '0.8.16'
fs_permissions = [{ access = "read", path = "XykCurve.json"}]

[profile.default.fuzz]
runs = 512

# See more config options https://github.com/foundry-rs/foundry/tree/master/config

